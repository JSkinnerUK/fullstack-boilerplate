{
  "name": "@project-backend/db",
  "version": "1.0.0",
  "main": "index.ts",
  "license": "MIT",
  "devDependencies": {
    "prisma": "^5.3.1"
  },
  "dependencies": {
    "@prisma/client": "^5.3.1"
  },
  "scripts": {
    "generate": "yarn prisma generate --schema=./prisma/schema.prisma",
    "migrate:save": "yarn prisma migrate dev --schema=./prisma/schema.prisma",
    "migrate:up": "yarn prisma migrate deploy --schema=./prisma/schema.prisma",
    "studio": "npx prisma studio"
  }
}
